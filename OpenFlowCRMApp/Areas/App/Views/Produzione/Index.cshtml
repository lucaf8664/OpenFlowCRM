@{
    @model IEnumerable<OpenFlowCRMModels.Models.Macchine>

}

<head>
    <style type="text/css">
        .button:hover,
        .label.info:hover {
            cursor: pointer;
        }

    </style>
    <style>
        .bgimg {
            display: block;
            height: 100%;
            background-image: url('@Url.Content("~/Images/MapLayout.jpg")');
            background-repeat: no-repeat;
            margin: 0% 25%;
        }

    </style>
</head>
<body>@*
        <div class="bgimg">
            div with background
        </div>
    *@
    @*<img src="~/Images/ZoomedOpaqueGroundLayout.png" alt="MyCompany 4.0">*@
    <svg class="bgimg" width="100%" height="800" scontentStyleType="background-image: linear-gradient(to right, rgba(255,0,0,0), rgba(255,0,0,0));">

    @foreach (var machine in Model)
    {
    @await Html.PartialAsync("_MachineButton", machine)
    ;
    }
    </svg>

</body>